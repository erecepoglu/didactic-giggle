# didactic-giggle

This repository contains the `didactic-giggle` project.

## Getting started

Clone the repository and inspect the files:

```bash
git clone https://github.com/erecepoglu/didactic-giggle.git
cd didactic-giggle
ls -la
```
